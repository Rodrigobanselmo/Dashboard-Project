import React from 'react';
import { NavLogoSC } from './Logo';

export const NavLogo = (props) => {

  return (
            <NavLogoSC {...props}>
              Simple<span>SST</span>
            </NavLogoSC>
  );
}
